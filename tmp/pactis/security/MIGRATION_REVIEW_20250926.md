# Migration Review: 20250926191644 + 20250926191646

Date: 2026-03-26 (UTC)
Reviewer: Codex

## Files reviewed
- `priv/repo/migrations/20250926191644_initial_migration_extensions_1.exs`
- `priv/repo/migrations/20250926191646_initial_migration.exs`
- `priv/repo/seeds.exs`

## Verdict (high confidence)
These migrations appear to be **Ash Postgres generated baseline migrations**, not ad-hoc hostile SQL.

## Evidence

### 1) Generator marker in both migration modules
Both files explicitly state they were autogenerated by `mix ash_postgres.generate_migrations` in their module docs.

### 2) Extension/function migration matches known Ash/Ecto patterns
`20250926191644_initial_migration_extensions_1.exs` installs common extension/function plumbing:
- `uuid-ossp`, `citext`
- Ash helper SQL functions (`ash_elixir_or`, `ash_elixir_and`, `ash_raise_error`, etc.)
- UUID v7 helper functions

This is expected in advanced Ash Postgres setups.

### 3) Initial schema migration is large because resource surface is large
`20250926191646_initial_migration.exs` creates many tables/resources in one generated baseline migration. Large output is expected when snapshotting many Ash resources at once.

### 4) Seeds file has some SQL, but it is bounded utility SQL
`priv/repo/seeds.exs` uses SQL for table existence checks and conditional delete operations; this is operational convenience logic for development seeding and not inherently suspicious by itself.

## Recommended cleanup path (if you want a cleaner reboot)
1. Keep these migration files for reproducibility until reset day.
2. On reset day, regenerate a clean baseline from current Ash resources in a fresh database:
   - archive old migrations to `priv/repo/migrations_legacy/`
   - run `mix ash_postgres.generate_migrations`
   - verify `mix ecto.migrate` and `mix test`
3. Keep JSON-LD as contract/source layer; continue DB projection/import as operational layer.

## Risk note
The biggest risk in this area is **schema drift**, not hostile SQL in these two files. Ensure one authoritative migration lineage and avoid manual edits to generated migration files.
