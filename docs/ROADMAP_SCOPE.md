# Active Scope (2026-04-14)

The active implementation scope is intentionally narrowed to:

- deterministic hashing and tree sealing
- storage/policy/daemon integration
- Yata protocol/addressing and regression coverage
- compiler-diagnostics to Yata/Jules swap-in pipeline

The following tracks are de-scoped from active implementation:

- Merkle-style expansion work beyond current tree sealing behavior
- FSM-based cognitive compiler orchestration
- computation/offload/distributed planner implementation roadmap

Historical documents for those tracks remain in `docs/` for reference only.

For a curated compiler/runtime handoff set to Mu language, see:

- `docs/MU_LANG_COMPILER_HANDOFF.md`
